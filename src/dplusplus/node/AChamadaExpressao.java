/* This file was generated by SableCC (http://www.sablecc.org/). */

package dplusplus.node;

import java.util.*;
import dplusplus.analysis.*;

@SuppressWarnings("nls")
public final class AChamadaExpressao extends PExpressao
{
    private final LinkedList<TId> _prefixos_ = new LinkedList<TId>();
    private TId _metodo_;
    private final LinkedList<PExpressao> _expressao_ = new LinkedList<PExpressao>();

    public AChamadaExpressao()
    {
        // Constructor
    }

    public AChamadaExpressao(
        @SuppressWarnings("hiding") List<?> _prefixos_,
        @SuppressWarnings("hiding") TId _metodo_,
        @SuppressWarnings("hiding") List<?> _expressao_)
    {
        // Constructor
        setPrefixos(_prefixos_);

        setMetodo(_metodo_);

        setExpressao(_expressao_);

    }

    @Override
    public Object clone()
    {
        return new AChamadaExpressao(
            cloneList(this._prefixos_),
            cloneNode(this._metodo_),
            cloneList(this._expressao_));
    }

    @Override
    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAChamadaExpressao(this);
    }

    public LinkedList<TId> getPrefixos()
    {
        return this._prefixos_;
    }

    public void setPrefixos(List<?> list)
    {
        for(TId e : this._prefixos_)
        {
            e.parent(null);
        }
        this._prefixos_.clear();

        for(Object obj_e : list)
        {
            TId e = (TId) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._prefixos_.add(e);
        }
    }

    public TId getMetodo()
    {
        return this._metodo_;
    }

    public void setMetodo(TId node)
    {
        if(this._metodo_ != null)
        {
            this._metodo_.parent(null);
        }

        if(node != null)
        {
            if(node.parent() != null)
            {
                node.parent().removeChild(node);
            }

            node.parent(this);
        }

        this._metodo_ = node;
    }

    public LinkedList<PExpressao> getExpressao()
    {
        return this._expressao_;
    }

    public void setExpressao(List<?> list)
    {
        for(PExpressao e : this._expressao_)
        {
            e.parent(null);
        }
        this._expressao_.clear();

        for(Object obj_e : list)
        {
            PExpressao e = (PExpressao) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._expressao_.add(e);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._prefixos_)
            + toString(this._metodo_)
            + toString(this._expressao_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._prefixos_.remove(child))
        {
            return;
        }

        if(this._metodo_ == child)
        {
            this._metodo_ = null;
            return;
        }

        if(this._expressao_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<TId> i = this._prefixos_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((TId) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        if(this._metodo_ == oldChild)
        {
            setMetodo((TId) newChild);
            return;
        }

        for(ListIterator<PExpressao> i = this._expressao_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PExpressao) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
